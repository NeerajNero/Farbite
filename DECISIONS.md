# DECISIONS.md — assumptions recorded by the agent (PLAN.md §0.4)

## Phase 0 (2026-09-22)

1. **NestJS 12 / Next.js 16.3.5** — PLAN says "NestJS 11+" and "Next.js 15+"; the official CLIs
   scaffold 12 and 16. Took the latest. Next 16 has breaking changes vs older docs — see
   `apps/web/AGENTS.md` (generated by `next dev`, points at `node_modules/next/dist/docs/`).
2. **Vitest** — the Nest 12 CLI scaffolds Vitest (not Jest), answering Phase 1's "whichever is
   simpler" question. Web tests (later) will also use Vitest.
3. **`GET /v1/ping`** — not in PLAN §10. Added as a Phase 0 smoke endpoint: the only guarded
   route until Phase 2, used by the status page and e2e tests to prove `InternalKeyGuard` +
   `ActorGuard`. Remove or keep once real routes exist.
4. **Shared package name `@weekend-drop/shared`**, built to `dist/` with tsc (commonjs). Both
   apps consume it as a workspace dep; `pnpm build` builds it first.
5. **`DATABASE_URL` optional in Phase 0** — env validation allows it missing; `/health` reports
   `db: false`. It becomes required in Phase 1.
6. **Env in tests** — `ConfigModule` validates at import time, so e2e test env lives in
   `vitest.config.e2e.ts` (`test.env`), and the module sets `ignoreEnvFile` when `NODE_ENV=test`.
7. **Error codes** — added `UNAUTHORIZED`, `VALIDATION_ERROR`, `INTERNAL` to the §10 list (needed
   for the envelope on 401/400/500; the web app doesn't have to switch on them).
8. **Lint** — kept each scaffold's default (oxlint for api, eslint for web) rather than unifying.
9. **Agent tooling predates Phase 0** — `.claude/` skills/agents/hooks + Context Protocol were
   imported from a prior project on 2026-09-22 and reconciled with PLAN.md (§0.14, §7.1 note).
   Where imported skills assume a generated SDK or SQL-first migrations, PLAN.md wins.
10. **apps/api naming** — kept PLAN's `apps/api` (agent tooling docs updated from `apps/backend`).
